// Owner rating UI is temporarily disabled.
// The owner_ratings table remains intact in the DB for future use.

export default function RateOwnerButton(_props: {
  appointmentId: string;
  ownerId: string;
  ownerName: string;
  existingRating?: number;
  existingNote?: string;
}) {
  return null;
}
